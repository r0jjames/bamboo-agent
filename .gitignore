# maven
target/
# rendered manifests (may embed values); regenerate from templates
*.rendered.yaml
# local env
.env
*.local
# os
.DS_Store
# ide
.idea/
*.iml
# bamboo specs publish token
.credentials
